{
	"name": "wiki0",
	"version": "0.0.0",
	"description": "Local-first AI memory as a Markdown wiki backed by SQLite search and MCP tools",
	"keywords": [
		"ai-memory",
		"cli",
		"knowledge-base",
		"mcp",
		"second-brain",
		"sqlite",
		"wiki"
	],
	"homepage": "https://github.com/spences10/wiki0#readme",
	"bugs": {
		"url": "https://github.com/spences10/wiki0/issues"
	},
	"license": "MIT",
	"author": "Scott Spence <me@scottspence.com>",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/spences10/wiki0.git"
	},
	"type": "module",
	"private": true,
	"scripts": {
		"build": "pnpm --filter \"./packages/*\" run build:self && pnpm --filter wiki0-web run build",
		"check": "pnpm --filter \"./packages/*\" run build:self && pnpm --filter \"./packages/*\" run check:self && pnpm --filter wiki0-web run check",
		"dev:web": "pnpm --filter wiki0-web run dev",
		"format": "pnpm --filter wiki0-web exec vp check --fix && prettier --write .",
		"lint": "pnpm --filter wiki0-web run lint",
		"preview:web": "pnpm --filter wiki0-web run preview",
		"test": "pnpm --filter \"./packages/*\" --if-present run test:self && pnpm --filter wiki0-web run test"
	},
	"devDependencies": {
		"@changesets/cli": "^2.31.0",
		"@types/node": "catalog:",
		"prettier": "^3.7.3",
		"typescript": "catalog:",
		"vite-plus": "catalog:",
		"vitest": "catalog:"
	},
	"engines": {
		"node": ">=24.15.0"
	},
	"packageManager": "pnpm@11.3.0"
}
